type: object
required:
  - action
  - link
properties:
  action:
    type: string
    description: action that must be handled in module, must be "get_users"
    example: "get_users"
  link:
    type: string
    description: domain where module has to get users
    example: "domain.com"
